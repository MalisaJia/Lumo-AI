# 数据库连接（默认 backend/lumo.db）
DATABASE_URL=sqlite+aiosqlite:///./backend/lumo.db
# 后端端口
PORT=8000
# API Key 加密主密钥（64 位 hex，留空则首次启动自动生成并写入 .env）
MASTER_KEY=
# 允许私网 SearXNG 地址（自建实例常部署在 127.0.0.1；默认 false 拒绝私网）
ALLOW_PRIVATE_SEARXNG=false
